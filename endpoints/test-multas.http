@baseUrl = {{baseUrl}}

### Login first to get token
# @name login
POST {{baseUrl}}/auth/login
Content-Type: application/json

{
    "email": "admin@admin.com",
    "password": "123123"
}

@authToken = {{login.response.body.access_token}}

### Multas Management Endpoints

### Create a new multa
# @name createMulta
POST {{baseUrl}}/multas
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "amount": 100,
  "description": "Tardanza a reunión",
  "juntaId": "{{juntaId}}",
  "memberId": "{{memberId}}"
}

@newMultaId = {{createMulta.response.body.id}}

### Get multas for a junta
GET {{baseUrl}}/multas/junta/{{juntaId}}
Authorization: Bearer {{authToken}}

### Get specific multa
GET {{baseUrl}}/multas/{{multaId}}
Authorization: Bearer {{authToken}}

### Update multa status
PUT {{baseUrl}}/multas/{{multaId}}
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "status": "PAID"
}

### Variables for testing
@juntaId = your-junta-id-here
@memberId = your-member-id-here
@multaId = {{newMultaId}} # Uses the ID from createMulta response, or replace with your multa ID
